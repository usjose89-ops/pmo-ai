services:
  - type: web
    name: pmo-backend
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres:Chile2026#$%@db.jcmjepvpzygtsmpvudob.supabase.co:5432/postgres
      - key: CORS_ORIGINS
        value: "https://pmo-avanziahub.vercel.app,http://localhost:3000"
